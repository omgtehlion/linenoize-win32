## TODO

- Use `getenv` / `getenvW` instead of `getEnvVarOwned`
- Support single \[Esc\] keypress (see fd_read_char in original C source)
- Disable Ctrl+L (configurable)
- Ignore F1-F12 keys (do not print random letters)


